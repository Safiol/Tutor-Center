const queueManager = require("../utils/sharedQueueManager");

class emailNotificationSystem extends I_NoticicationSystem{
    constructor(){
        //Code to initialize email sender
    }

    async sendAlert(address) {
        //code to send the email
    }
}